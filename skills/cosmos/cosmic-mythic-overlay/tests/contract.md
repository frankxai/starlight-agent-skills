# Contract test — cosmic-mythic-overlay

Given the input in [`../examples/example-01.md`](../examples/example-01.md), the
output MUST satisfy:

- [ ] `astronomy_layer` is accurate and unchanged from the source facts.
- [ ] `mythic_overlay` does not contradict the astronomy; the two are separable.
- [ ] `image_credit` preserves the original credit.
- [ ] `canon_attribution` notes Arcanea CC-BY-NC.
- [ ] Register is mythic while the science stays accurate.
- [ ] Parent artifact carries the "Built on SIP" attestation.
