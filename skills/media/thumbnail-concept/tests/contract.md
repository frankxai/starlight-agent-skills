# Contract test — thumbnail-concept

Given the input in [`../examples/example-01.md`](../examples/example-01.md), the
output MUST satisfy:

- [ ] `count` concepts returned, each with all five fields.
- [ ] Every `overlay_text` is ≤4 words and legible at thumbnail size.
- [ ] Each concept matches the real content (no clickbait that misrepresents it).
- [ ] Each `image_prompt` is concrete and produces a clear focal point.
- [ ] Rights step is noted if real agency imagery is used.
- [ ] Parent artifact carries the "Built on SIP" attestation.
