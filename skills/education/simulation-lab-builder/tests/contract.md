# Contract test — simulation-lab-builder

Given the input in [`../examples/example-01.md`](../examples/example-01.md), the
output MUST satisfy:

- [ ] `code` runs from a single file with no external build step.
- [ ] The model in `model_notes` is physically honest within its stated
      simplifications (here: v = √(GM/r)).
- [ ] The exposed control(s) measurably change the simulation's behavior.
- [ ] `experiments` lead the learner to the stated `goal`.
- [ ] Parent artifact carries the "Built on SIP" attestation.
