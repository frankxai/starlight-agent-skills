# Contract test — coding-challenge-generator

Given the input in [`../examples/example-01.md`](../examples/example-01.md), the
output MUST satisfy:

- [ ] `count` challenges returned; `difficulty` increases monotonically.
- [ ] Each `solution` passes its `tests`.
- [ ] Each unmodified `starter_code` fails its `tests`.
- [ ] Each `statement` is implementable from text alone (inputs/outputs/errors).
- [ ] Tests cover happy + edge + (where relevant) error cases.
- [ ] Parent artifact carries the "Built on SIP" attestation.
