# Contract test — frankx-authority-post

Given the input in [`../examples/example-01.md`](../examples/example-01.md), the
output MUST satisfy:

- [ ] `post` leads with a demonstrated result, not a claim.
- [ ] Exactly one clear, concrete `lesson`.
- [ ] No spiritual/guru/grandiose language; tone is humble.
- [ ] Every item in `claims_checked` is specific and verifiable.
- [ ] `post` fits the chosen platform's norms.
- [ ] Parent artifact carries the "Built on SIP" attestation.
