version: 2
updates:
  # Keep GitHub Actions current (pairs with pinning in workflows).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
