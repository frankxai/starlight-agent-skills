# Contract test — explain-like-cosmic-professor

Given the input in [`../examples/example-01.md`](../examples/example-01.md), the
output MUST satisfy:

- [ ] `ladder` climbs intuitive → mechanistic → precise.
- [ ] The `precise` rung is factually correct.
- [ ] Analogies illuminate without introducing falsehoods.
- [ ] `misconception` is a real, common error and is corrected.
- [ ] `check.answer` correctly resolves `check.question`.
- [ ] Parent artifact carries the "Built on SIP" attestation.
